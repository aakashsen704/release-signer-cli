This file simulates a compiled release binary (e.g. app.exe, app.bin).
Version: 1.0.0
Build date: 2026-08-14
In a real pipeline this would be actual compiled/packaged bytes -
the signer treats any file as an opaque sequence of bytes, so it
works identically for binaries, tarballs, container images, etc.
abc